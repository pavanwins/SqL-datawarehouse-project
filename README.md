# SqL-datawarehouse-project
SQL Data Warehouse project built using Bronze, Silver, and Gold architecture. Raw data is ingested into the Bronze layer, cleaned and transformed in the Silver layer, and analytics-ready datasets with KPIs and insights are created in the Gold layer using SQL.
